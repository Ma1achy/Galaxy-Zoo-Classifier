# Galaxy Zoo Classifier — Python dependencies
# Exact versions from the environment the project was developed against (the pip
# section of projectenv.yaml, ~May 2024). Fully pinned, lockfile-style. Install
# with uv:
#
#     uv venv && uv pip install -r requirements.txt
#
# PLATFORM NOTE
# The original environment was built on Apple Silicon. The two Mac-only wheels
# (tensorflow-macos, and tensorflow-metal — Apple's Metal GPU plugin) are kept
# below but gated to macOS, so they install on the original machine and are
# skipped in the Linux dev container, which uses the standard tensorflow wheel.
# For an NVIDIA GPU host, install `tensorflow[and-cuda]==2.16.1` instead.
#
# setuptools/wheel are pinned to the conda-era versions because uv venvs are bare
# by default; pip itself is omitted (uv manages installs, the venv doesn't need it).

absl-py==2.1.0
astunparse==1.6.3
certifi==2024.2.2
charset-normalizer==3.3.2
contourpy==1.2.0
cramjam==2.8.3
cycler==0.12.1
fastparquet==2024.2.0
flatbuffers==24.3.7
fonttools==4.50.0
fsspec==2024.3.1
gast==0.5.4
google-pasta==0.2.0
grpcio==1.62.1
h5py==3.10.0
idna==3.6
joblib==1.3.2
keras==3.1.1
kiwisolver==1.4.5
libclang==18.1.1
markdown==3.6
markdown-it-py==3.0.0
markupsafe==2.1.5
matplotlib==3.8.3
mdurl==0.1.2
ml-dtypes==0.3.2
namex==0.0.7
numpy==1.26.4
opt-einsum==3.3.0
optree==0.11.0
packaging==24.0
pandas==2.2.1
pillow==10.2.0
protobuf==4.25.3
pyarrow==15.0.2
pygments==2.17.2
pyparsing==3.1.2
python-dateutil==2.9.0.post0
pytz==2024.1
requests==2.31.0
rich==13.7.1
scikit-learn==1.4.1.post1
scipy==1.12.0
setuptools==68.2.2
six==1.16.0
tensorboard==2.16.2
tensorboard-data-server==0.7.2
tensorflow==2.16.1
tensorflow-io-gcs-filesystem==0.36.0
tensorflow-macos==2.16.1 ; sys_platform == "darwin"
tensorflow-metal==1.1.0 ; sys_platform == "darwin"
termcolor==2.4.0
threadpoolctl==3.4.0
typing-extensions==4.10.0
tzdata==2024.1
urllib3==2.2.1
werkzeug==3.0.1
wheel==0.41.2
wrapt==1.16.0
